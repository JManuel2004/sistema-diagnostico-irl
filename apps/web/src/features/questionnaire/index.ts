/**
 * Superficie pública del feature `questionnaire`.
 *
 * Stage 1 intencionalmente no exporta nada. La estructura de carpetas
 * (`api/`, `components/`, `hooks/`, `store/`, `utils/`) ya está en su
 * lugar para que el trabajo de Stage 2 sobre HU-07 / HU-08 / HU-09
 * aterrice dentro del shape acordado sin inventar un layout nuevo.
 *
 * Per CLAUDE.web.md, solo lo que se necesite fuera del feature se
 * exporta aquí. Stage 2 exportará como mínimo:
 *
 *   export { QuestionnaireForm } from './components/QuestionnaireForm';
 *   export { useQuestionnaireDraft } from './hooks/useQuestionnaireDraft';
 *
 * Ver `apps/web/docs/MODULES.md` → "questionnaire" para el plan completo.
 */
export {};
