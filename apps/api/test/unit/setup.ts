import 'reflect-metadata';

/**
 * Setup for the `unit` Jest project. Unit tests are synchronous and pure
 * where possible; they never spin up Docker containers, hit databases,
 * or perform IO. Keep this file minimal.
 */
